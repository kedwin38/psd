{
  "name": "@psd-studio/canvas-renderer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"(no lint configured for canvas-renderer)\"",
    "test": "vitest run"
  },
  "dependencies": {
    "@psd-studio/scene-graph": "workspace:*"
  },
  "devDependencies": {
    "@napi-rs/canvas": "^1.0.9",
    "@psd-studio/psd-engine": "workspace:*",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
